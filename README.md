# one.py

## Usage:
```py
>>> import one
>>> one + one
<module 'two.py'>
>>> import two
>>> (one + two) ** two
<module 'nine.py'>
>>> import math
>>> two * math.pi
<module 'tau.py'>
```

Now as a `pip` installable package!
```
pip install one-dot-py
```
